/**
 *  Simple and overused function to test
 * @param x First number to add
 * @param y Second number to add
 * @returns addition of x + y
 */
export default function sum(x: number, y: number) {
  return x + y;
}
